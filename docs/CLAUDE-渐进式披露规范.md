# CLAUDE.md 渐进式披露规范

> **让所有 Agent（Claude/OpenAI/Gemini）自动遵循文档组织规则**

---

## 🎯 目标

确保**所有 AI Agent**在添加/更新文档时：
- ✅ 自动使用渐进式披露结构
- ✅ 在适当位置添加内容
- ✅ 使用链接而非重复内容
- ✅ 不需要手动整理

---

## 📋 文档组织规则

### 规则 1: 内容分层原则

```
┌─────────────────────────────────────┐
│ CLAUDE.md（主文件）           │
│ - 快速参考表                  │
│ - 链接到详细文档              │
│ - ≤ 100 行                     │
├─────────────────────────────────────┤
│ reference/*.md（参考文档）       │
│ - 详细说明（100-300 行）      │
│ - 按需加载                     │
├─────────────────────────────────────┤
│ scripts/*.py（工具脚本）        │
│ - 可执行代码                  │
│ - 不消耗上下文 token           │
└─────────────────────────────────────┘
```

**决策树**：
```
内容是什么？
│
├─ 快速命令/表格？
│  └─ → CLAUDE.md
│
├─ 简短说明（< 5 行）？
│  └─ → CLAUDE.md
│
├─ 详细步骤（≥ 5 行）？
│  └─ → reference/*.md
│
├─ 代码示例（> 5 行）？
│  └─ → reference/*.md
│
└─ 可执行脚本？
   └─ → scripts/*.py
```

### 规则 2: CLAUDE.md 结构要求

```markdown
# <项目名称>

<一句话描述>

---

## 🛠️ 快速命令

| 命令 | 用途 |
|------|------|
| `<cmd>` | `<desc>` |

---

## 📚 详细文档

| 主题 | 参考文档 |
|------|----------|
| **环境设置** | [reference/setup.md](reference/setup.md) |
| **架构** | [reference/architecture.md](reference/architecture.md) |
| **代码风格** | [reference/code-style.md](reference/code-style.md) |

---

## 🎯 快速参考

| 我想... | 用什么 |
|---------|---------|
| 安装依赖 | `uv sync` |
| 运行测试 | `uv run -m pytest` |
```

**限制**：
- CLAUDE.md ≤ 100 行
- 只包含：快速参考表 + 详细文档链接
- 不包含：长篇说明、详细步骤

### 规则 3: 链接格式

```markdown
# ✅ 正确
[reference/setup.md](reference/setup.md)

# ❌ 错误（绝对路径）
[reference/setup.md](/home/user/project/reference/setup.md)

# ❌ 错误（@ 语法强制加载）
@reference/setup.md
```

### 规则 4: reference/ 文件命名

| 类型 | 命名规范 | 示例 |
|------|----------|------|
| 环境配置 | `setup.md`, `environment.md` | ✅ |
| 架构设计 | `architecture.md` | ✅ |
| 代码规范 | `code-style.md`, `conventions.md` | ✅ |
| 测试指南 | `testing.md` | ✅ |
| API 文档 | `api.md`, `endpoints.md` | ✅ |
| 常见问题 | `troubleshooting.md`, `gotchas.md` | ✅ |
| 部署指南 | `deployment.md` | ✅ |

**避免**：
- ❌ `guide.md`, `docs.md`, `info.md`（太模糊）
- ❌ `reference.md`, `details.md`（不描述性）

---

## 🔄 添加新内容的工作流

### Step 1: 识别内容类型

**问自己**：
1. 这是快速命令吗？ → CLAUDE.md
2. 这是详细说明吗？ → reference/
3. 这是可执行脚本吗？ → scripts/

### Step 2: 选择文件位置

```
添加新功能文档：
                    │
        ┌─────────┴─────────┐
        │                   │
    简单？            详细？
        │                   │
    CLAUDE.md       reference/
        │                   │
        └─────────┬─────────┘
                  │
                  脚本？
                  │
              scripts/
```

### Step 3: 更新 CLAUDE.md

**如果添加到 CLAUDE.md**：
```markdown
## 🛠️ 快速命令

| 命令 | 用途 |
|------|------|
| `<新命令>` | `<新描述>` |
```

**如果添加到 reference/**：
```markdown
## 📚 详细文档

| 主题 | 参考文档 |
|------|----------|
| **<新主题>** | [reference/<新文件>.md](reference/<新文件>.md) |
```

### Step 4: 创建/更新参考文件

```bash
# 创建新参考文件
touch reference/<新文件>.md

# 或更新现有文件
vim reference/<现有文件>.md
```

### Step 5: 验证

```bash
# 1. 检查 CLAUDE.md 行数
wc -l CLAUDE.md
# 目标：≤ 100 行

# 2. 检查链接格式
grep -o '\[.*\](reference/[^)]*)' CLAUDE.md

# 3. 检查文件存在
for link in $(grep -oP '\[.*\]\(\K[^)]*\)' CLAUDE.md); do
    file="reference/${link})"
    if [ ! -f "$file" ]; then
        echo "❌ 链接目标不存在：$file"
    fi
done
```

---

## 🤖 Agent 指令语言

### 在 CLAUDE.md 中使用明确指令

```markdown
## 📝 文档维护规范

**必需**：所有文档更新必须遵循渐进式披露原则

**无例外**：
- 详细内容 → reference/
- 快速参考 → CLAUDE.md
- 脚本 → scripts/

**禁止**：
- ❌ 在 CLAUDE.md 中添加 >5 行的说明
- ❌ 重复内容（同时放在 CLAUDE.md 和 reference/）
- ❌ 使用绝对路径链接
```

### 使用验证清单

```markdown
## 更新文档时检查清单

在提交任何文档更改前，确认：

- [ ] CLAUDE.md ≤ 100 行
- [ ] 详细内容在 reference/
- [ ] 链接使用相对路径
- [ ] 新文件已添加到详细文档表
- [ ] 验证命令可执行
- [ ] 运行 `check-claude-md.sh` 验证
```

---

## 🧪 验证脚本

创建自动检查脚本：`scripts/check-claude-md.sh`

```bash
#!/usr/bin/env bash
set -e

echo "🔍 检查 CLAUDE.md 渐进式披露合规性..."

# 检查 1: 行数限制
LINES=$(wc -l < CLAUDE.md)
if [ $LINES -gt 100 ]; then
    echo "⚠️  警告：CLAUDE.md 超过 100 行（当前：$LINES）"
    echo "建议：将详细内容移到 reference/"
else
    echo "✅ 行数正常：$LINES"
fi

# 检查 2: 链接目标存在
echo "🔍 检查链接目标..."
MISSING=0
grep -oP '\[.*\]\(\K[^)]*\)' CLAUDE.md | while read link; do
    file="reference/${link})"
    if [ ! -f "$file" ]; then
        echo "❌ 链接目标不存在：$file"
        MISSING=$((MISSING + 1))
    fi
done

if [ $MISSING -eq 0 ]; then
    echo "✅ 所有链接目标存在"
else
    echo "❌ 发现 $MISSING 个缺失链接"
    exit 1
fi

# 检查 3: 重复内容
echo "🔍 检查重复内容..."
# （这里可以添加更复杂的检查）

echo "✅ 检查完成！"
```

**使用**：
```bash
chmod +x scripts/check-claude-md.sh
./scripts/check-claude-md.sh
```

---

## 📊 示例对比

### ❌ 错误示例

```markdown
# CLAUDE.md（186 行 - 太长！）

## 快速命令

npm install
npm install
npm test

## 数据库连接详细步骤（50 行详细说明）

1. 打开终端
2. 输入 psql
3. 输入密码
...

## API 文档（100 行 API 文档）

所有端点列表...
```

**问题**：
- ❌ CLAUDE.md 过长
- ❌ 详细内容应该在 reference/
- ❌ 没有使用链接

### ✅ 正确示例

```markdown
# CLAUDE.md（42 行 - 合适）

## 🛠️ 快速命令

| 命令 | 用途 |
|------|------|
| `npm install` | 安装依赖 |
| `npm test` | 运行测试 |

## 📚 详细文档

| 主题 | 参考文档 |
|------|----------|
| **环境设置** | [reference/setup.md](reference/setup.md) |
| **数据库连接** | [reference/database.md](reference/database.md) |
| **API 文档** | [reference/api.md](reference/api.md) |
```

**优势**：
- ✅ CLAUDE.md 简洁
- ✅ 详细内容在参考文档
- ✅ 使用链接按需加载

---

## 🎯 快速决策表

| 场景 | 放哪里？ | 为什么？ |
|--------|----------|--------|
| 添加新命令 | CLAUDE.md 快速命令表 | 快速访问 |
| 添加 >5 行说明 | reference/<主题>.md | 节省 token |
| 添加代码示例 | CLAUDE.md（如果 <5 行） | 快速参考 |
| 添加代码示例 | reference/<主题>.md（如果 >5 行） | 按需加载 |
| 添加可执行脚本 | scripts/<工具>.py | 不消耗上下文 |
| 更新现有内容 | 直接编辑对应文件 | 保持组织 |

---

> **版本**: 1.0.0
> **最后更新**: 2026-02-14
