# runStory - 项目指导

本文件为 Claude Code (claude.ai/code)、OpenAI Codex、Gemini 等 AI Agent 提供项目指导。

**重要**：所有文档更新必须遵循[渐进式披露规范](CLAUDE-渐进式披露规范.md)。

**文档结构**：
- CLAUDE.md ≤ 100 行（快速参考）
- 详细内容 → reference/*.md（按需加载）
- 可执行脚本 → scripts/*.py

---

## 📝 文档维护规范

### 必需规则

**所有 AI Agent 在添加/更新文档时必须遵守**：

1. **内容分层**：
   - CLAUDE.md ≤ 100 行（快速参考）
   - 详细内容 → reference/*.md
   - 可执行脚本 → scripts/*.py

2. **禁止行为**：
   - ❌ 在 CLAUDE.md 添加 >5 行说明
   - ❌ 重复内容（同时放 CLAUDE.md 和 reference/）
   - ❌ 使用绝对路径链接

3. **验证要求**：
   - 提交前运行 `./scripts/check-claude-md.sh`
   - 确保所有链接目标存在
   - 确保行数 ≤ 100

### 例外情况

**无例外** - 即使是"快速修复"或"临时说明"也必须遵守。

---

## 🛠️ 快速命令

| 命令 | 用途 |
|------|------|
| `uv sync` | 安装依赖 |
| `uv run -m pytest tests/ -v` | 运行测试 |
| `uv run -m black src/ tests/` | 格式化代码 |
| `uv run -m ruff check --fix src/` | 检查代码 |

---

## 📚 详细文档

| 主题 | 参考文档 |
|------|----------|
| **环境设置** | [reference/setup.md](reference/setup.md) |
| **包管理** | [reference/package-management.md](reference/package-management.md) |
| **运行测试** | [reference/testing.md](reference/testing.md) |
| **代码质量** | [reference/code-quality.md](reference/code-quality.md) |
| **架构** | [reference/architecture.md](reference/architecture.md) |
| **代码风格** | [reference/code-style.md](reference/code-style.md) |
| **常见陷阱** | [reference/gotchas.md](reference/gotchas.md) |

**注意**：[详细文档索引](reference/)查看所有可用参考文档。

---

## 🎯 快速参考

| 我想... | 用什么 |
|---------|---------|
| 安装依赖 | `uv sync` |
| 运行测试 | `uv run -m pytest tests/ -v` |
| 格式化代码 | `uv run -m black src/ tests/` |
| 检查代码 | `uv run -m ruff check --fix src/` |
| 创建虚拟环境 | `python -m venv .venv` |
| 激活环境 | `source .venv/bin/activate` |

---

## 📖 项目结构

```
runStory/
├── src/                    # 源代码
├── tests/                  # 测试套件
├── scripts/                 # 实用脚本
├── reference/               # 详细参考文档（渐进式披露）
├── docs/                   # 项目文档
└── CLAUDE.md               # 本文件
```

---

## 📋 更新文档时检查清单

在提交任何文档更改前，确认：

- [ ] CLAUDE.md ≤ 100 行
- [ ] 详细内容在 reference/
- [ ] 链接使用相对路径 `[文件名](reference/文件名.md)`
- [ ] 新文件已添加到详细文档表
- [ ] 验证命令可执行
- [ ] 运行 `./scripts/check-claude-md.sh` 验证
